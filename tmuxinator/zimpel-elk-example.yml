# ~/.tmuxinator/zimpel-elk-example.yml

name: zimpel-elk-example
root: /Users/virginio/projects/zimpel-elk-example

pre:
  - open "http://localhost:5601" 

windows:
  - editor: vim
  - shell: 
  - elasticsearch: "elasticsearch"
  - kibana: "kibana"
  - logstash: "logstash -f logstash.conf"
    #- filebeat: "sudo rm /usr/local/var/filebeat/registry && filebeat -e -c /Users/virginio/projects/zimpel-elk-example/filebeat.yml -d 'publish'"
  - filebeat: "sudo filebeat -e -c /Users/virginio/projects/zimpel-elk-example/filebeat.yml -d 'publish'"
